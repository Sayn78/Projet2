---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: viewer
rules:
  - apiGroups: ["*"]
    ressources: ["deployments", "configmap", "pods", "secrets", "services"]
    verbs: ["get", "list", "watch"]